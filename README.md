# Collections Portal — automated GitHub Pages

Any commit to `main` validates and deploys the portal with GitHub Actions. Individual collection updates do not require republishing this repository; the portal reads each live `summary.json`.
